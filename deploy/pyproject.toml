[project]
name = "localstack-ecs-sqs"
version = "0.1.0"
description = "Pulumi program for managing LocalStack ECS and SQS resources"
requires-python = ">=3.11"
dependencies = [
    "pulumi>=3.100.0,<4.0.0",
    "pulumi-aws>=6.0.0,<7.0.0",
]

[tool.uv]
package = true
